import 'package:flutter/material.dart';

class HabitTitle extends StatelessWidget {
  const HabitTitle({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
