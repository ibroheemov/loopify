name: betterloop
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+15

environment:
  sdk: ^3.6.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  shared_preferences: ^2.5.3
  smooth_page_indicator: ^1.2.1
  go_router: ^16.0.0
  flutter_svg: ^2.2.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0  
  path_provider: ^2.1.0
  uuid: ^4.5.1
  intl: ^0.20.2
  flutter_slidable: ^4.0.0
  flutter_local_notifications: ^19.4.0
  flutter_timezone: ^4.1.1
  lottie: ^3.1.0
  dartz: ^0.10.1
  equatable: ^2.0.7
  firebase_auth: ^5.7.0
  google_sign_in: ^6.3.0
  firebase_ui_auth: ^1.17.0
  firebase_core: ^3.15.2
  firebase_dynamic_links: ^6.1.10
  firebase_ui_oauth_google: ^1.4.2
  cloud_firestore: ^5.6.12
  fl_chart: ^1.0.0
  purchases_flutter: ^9.1.0
  in_app_update: ^4.2.3
  url_launcher: ^6.3.1
  share_plus: ^10.1.2
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^2.0.1
  build_runner: ^2.4.15
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.6

flutter_launcher_icons:
  android: true
  ios: true
  image_path_ios: "assets/images/icons/launcher.png"
  image_path_android: "assets/images/icons/launcher.png"
  adaptive_icon_background: "#FCE9EC" 
  adaptive_icon_foreground: "assets/ic_foreground.png"

flutter_native_splash:
  android: true 
  ios: true
  color: "#FCE9EC"
  image: assets/android_splash.png
  image_ios: assets/ios_splash.png

  
  android_12:
    android: true
    ios: true
    color: "#FCE9EC"
    image: assets/android_splash.png
    

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/onboarding/
    - assets/images/
    - assets/lottie/
    - assets/images/icons/   
    - assets/privacy_policy.txt
    - assets/faqs.txt

  fonts:
  # Icon Fonts
    - family: PopularIcons
      fonts:
        - asset: assets/fonts/popular.ttf
    - family: LifeStyleIcons
      fonts:
        - asset: assets/fonts/lifestyle.ttf
    - family: HealthIcons
      fonts:
        - asset: assets/fonts/health.ttf
    - family: DietIcons
      fonts:
        - asset: assets/fonts/diet.ttf
    - family: HobbyIcons
      fonts:
        - asset: assets/fonts/hobby.ttf
    - family: NegativeIcons
      fonts:
        - asset: assets/fonts/negative.ttf
    - family: EfficiencyIcons
      fonts:
        - asset: assets/fonts/efficiency.ttf
    - family: RelationshipIcons
      fonts:
        - asset: assets/fonts/relationship.ttf
    - family: GeneralIcons
      fonts:
        - asset: assets/fonts/general_icons.ttf
    - family: RandomIcons
      fonts:
        - asset: assets/fonts/random.ttf
   
  # Text Fonts
    - family: Nunito
      fonts:
        - asset: assets/fonts/Nunito-Regular.ttf
        - asset: assets/fonts/Nunito-Bold.ttf
          weight: 700
        - asset: assets/fonts/Nunito-Light.ttf
          weight: 300
        - asset: assets/fonts/Nunito-Italic.ttf
          style: italic